export const traceConfig = {
  radius: 0, // Radius of the trace effect (0 = single cell, 1+ = thicker trace)
  duration: 2000, // How long each trace cell lasts in milliseconds
  intensity: 0.8, // Base intensity of trace cells
  opacity: 1, // Opacity multiplier for trace cells
};
